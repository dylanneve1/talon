<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ backup policy: nothing leaves the device, either to cloud
     backup or in a device-to-device transfer. The app keeps a bridge token,
     an imported client certificate and a snapshot of recent chats in its
     private storage; a new or restored device pairs again instead. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
