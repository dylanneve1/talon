/**
 * Antigravity message handler barrel.
 *
 * One module today (`message.ts` — the turn orchestration); the
 * directory exists so the split mirrors codex's when the handler grows
 * a second responsibility.
 */

export { handleMessage } from "./message.js";
