# Generated artifacts (Gleam-emitted JS — drift-guarded against source in CI)
src/native/**/*.mjs
src/native/**/*.d.mts
src/native/*.mjs
src/native/*.d.mts

# Generated SQL embed (source of truth: src/storage/sql/*.sql — drift-guarded)
src/storage/sql/statements.generated.ts

# Captured CLI output used verbatim as test fixtures — reformatting them
# would stop them being a faithful record of what the binary emitted.
src/__tests__/fixtures/agy/*.json
src/__tests__/fixtures/agy/*.ndjson
